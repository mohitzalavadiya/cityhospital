const initVal = {
    isLoading : false,
    user : null,
    error : ''
}

export const SignupReducer = (state = initVal, action) => {
    switch(action.type){
        default : 
        return state
    }
}